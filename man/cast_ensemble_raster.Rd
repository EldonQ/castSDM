% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensemble.R
\name{cast_ensemble_raster}
\alias{cast_ensemble_raster}
\title{Raster-Based Ensemble Prediction}
\usage{
cast_ensemble_raster(
  fit,
  cv,
  raster_stack,
  output_dir,
  method = c("weighted", "best", "equal"),
  threshold_method = "maxTSS",
  models = NULL,
  mask = NULL,
  prefix = "",
  overwrite = FALSE,
  compression = "LZW",
  verbose = TRUE
)
}
\arguments{
\item{fit}{A \link{cast_fit} object.}

\item{cv}{A \link{cast_cv} object providing per-model evaluation metrics.}

\item{raster_stack}{A \code{terra::SpatRaster} whose layer names cover the
environmental variables in \code{fit$env_vars}.}

\item{output_dir}{Character. Directory for output rasters. Created if
it does not exist.}

\item{method}{Character. Ensemble strategy: \code{"weighted"} (default),
\code{"best"}, \code{"equal"}. See \code{\link[=cast_ensemble]{cast_ensemble()}}.}

\item{threshold_method}{Character. Binary threshold method. Default
\code{"maxTSS"}.}

\item{models}{Character vector or \code{NULL}. Models to use. Default all.}

\item{mask}{A \code{terra::SpatRaster} or \code{NULL}. If provided, prediction
is restricted to cells where mask is non-NA.}

\item{prefix}{Character. Filename prefix. Default \code{""}.}

\item{overwrite}{Logical. Overwrite existing output. Default \code{FALSE}.}

\item{compression}{Character. GeoTIFF compression. Default \code{"LZW"}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
A list with components:
\describe{
\item{hss_path}{File path to the HSS raster.}
\item{binary_path}{File path to the binary raster.}
\item{weights}{Named numeric vector of per-model weights.}
\item{threshold}{Binary classification threshold.}
\item{n_valid_cells}{Number of cells predicted.}
}
}
\description{
Generates ensemble habitat suitability (HSS) and binary suitability
rasters from a fitted model and cross-validation results. This is the
raster-native equivalent of \code{\link[=cast_ensemble]{cast_ensemble()}}: it reads a
\code{SpatRaster} stack, runs all models in memory, computes the weighted
ensemble, and writes GeoTIFF outputs.
}
\seealso{
\code{\link[=cast_ensemble]{cast_ensemble()}}, \code{\link[=cast_predict]{cast_predict()}}, \code{\link[=cast_project_raster]{cast_project_raster()}}
}
