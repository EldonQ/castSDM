% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch-resume.R
\name{cast_batch_resume}
\alias{cast_batch_resume}
\title{Resume an Interrupted \code{cast_batch()} Run}
\usage{
cast_batch_resume(
  output_dir,
  species_list,
  env_data = NULL,
  force = FALSE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{output_dir}{Top-level directory used by the original
\code{\link[=cast_batch]{cast_batch()}} call.}

\item{species_list}{Original named list of species data.frames.}

\item{env_data}{Optional shared environmental data.frame.}

\item{force}{Logical. If \code{TRUE}, ignore existing \code{cast_result.rds}
files and re-run all species (still benefits from per-step caches).
Default \code{FALSE}.}

\item{verbose}{Logical. Default \code{TRUE}.}

\item{...}{Forwarded to \code{\link[=cast_batch]{cast_batch()}} (must match the original call).}
}
\value{
A \code{cast_batch} object covering the species that were re-run.
Already-finished species are not re-loaded into the result; load
them manually from \verb{<output_dir>/<species>/cast_result.rds}.
}
\description{
Scans \code{output_dir} for species that already have a saved
\code{cast_result.rds} and re-runs \code{\link[=cast_batch]{cast_batch()}} only on the remainder.
Combined with the per-step \verb{.steps/<name>.rds} checkpoints written by
\code{\link[=cast_batch]{cast_batch()}}, this lets you recover from crashes, OOM kills, or
hard reboots without recomputing finished work.
}
\details{
Usage:
\itemize{
\item Same \code{species_list} and \code{output_dir} you originally passed to
\code{\link[=cast_batch]{cast_batch()}}.
\item Same \code{...} config arguments (changing them silently invalidates
step caches; in that case clear \code{output_dir} first).
}
}
\seealso{
\code{\link[=cast_batch]{cast_batch()}}
}
