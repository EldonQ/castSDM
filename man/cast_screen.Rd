% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variable-screen.R
\name{cast_screen}
\alias{cast_screen}
\title{Adaptive Multi-Criteria Variable Screening}
\usage{
cast_screen(
  dag,
  ate,
  data,
  response = "presence",
  min_vars = 5L,
  min_fraction = 0.5,
  num_trees = 300L,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{dag}{A \link{cast_dag} object.}

\item{ate}{A \link{cast_ate} object.}

\item{data}{A \code{data.frame} used for RF importance calculation.}

\item{response}{Character. Response column name. Default \code{"presence"}.}

\item{min_vars}{Integer. Minimum variables to retain. Default \code{5}.}

\item{min_fraction}{Numeric. Minimum fraction of variables to retain.
Default \code{0.5}.}

\item{num_trees}{Integer. Trees for RF importance. Default \code{300}.}

\item{seed}{Integer or \code{NULL}. Random seed.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
A \link{cast_screen} object.
}
\description{
Combines three complementary signals -- DAG centrality, ATE magnitude, and
RF permutation importance -- into a weighted composite score for variable
selection. Weights adapt based on DAG quality and ATE signal strength.
}
\details{
The composite score for each variable is:

\deqn{S = w_{dag} \cdot s_{dag} + w_{ate} \cdot s_{ate} + w_{imp} \cdot s_{imp}}

where weights adapt based on signal quality:
\itemize{
\item \eqn{w_{dag} = 0.15 + 0.15 \times dag\_quality}
\item \eqn{w_{ate} = 0.25 + 0.25 \times ate\_sig\_ratio}
\item \eqn{w_{imp} = 1 - w_{dag} - w_{ate}}
}

Variables are selected using k-means clustering on composite scores.
}
\seealso{
\code{\link[=cast_dag]{cast_dag()}}, \code{\link[=cast_ate]{cast_ate()}}, \code{\link[=cast_roles]{cast_roles()}}
}
