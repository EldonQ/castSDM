% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-from-config.R
\name{cast_run_from_config}
\alias{cast_run_from_config}
\title{Run a Full castSDM Pipeline from a YAML Config File}
\usage{
cast_run_from_config(config_path, ...)
}
\arguments{
\item{config_path}{Path to a YAML config file.}

\item{...}{Additional named arguments that override values from the
YAML (forwarded to \code{\link[=cast_batch]{cast_batch()}}).}
}
\value{
The \code{cast_batch} object returned by \code{\link[=cast_batch]{cast_batch()}} (or
\code{\link[=cast_batch_resume]{cast_batch_resume()}}).
}
\description{
One-stop entry point that reads a YAML configuration file, loads
per-species CSVs (and optionally a shared environmental grid),
constructs a \code{\link[=cast_worker_budget]{cast_worker_budget()}}, and invokes \code{\link[=cast_batch]{cast_batch()}} (or
\code{\link[=cast_batch_resume]{cast_batch_resume()}} if \code{run.resume: true} and partial output exists).
}
\details{
This is the recommended way to drive reproducible multi-species runs:
the YAML acts as a self-documenting record of every parameter, so a
run can be re-executed by anyone who has the same data files.

Generate a starter YAML with \code{\link[=cast_config_template]{cast_config_template()}}.
}
\section{YAML schema}{

Top-level keys recognised by \code{cast_run_from_config()}:
\describe{
\item{\code{run.output_dir}}{Where outputs are written.}
\item{\code{run.seed}}{Base seed (each species gets \code{seed + i}).}
\item{\code{run.parallel}}{Whether to run species in parallel.}
\item{\code{run.worker_budget.total_workers}}{Optional integer; passed to
\code{\link[=cast_worker_budget]{cast_worker_budget()}}. Omit to use \code{detectCores() - 1}.}
\item{\code{run.resume}}{If \code{TRUE}, calls \code{\link[=cast_batch_resume]{cast_batch_resume()}} when any
species in \code{output_dir} already has a saved \code{cast_result.rds}.}
\item{\code{species}}{Named map. Each entry has a \verb{file:} path to a CSV.}
\item{\code{env_data.file}}{Optional CSV with shared environmental data
for spatial HSS prediction.}
\item{\code{models}}{Character vector forwarded to \code{\link[=cast_batch]{cast_batch()}}.}
\item{\code{backend}}{\code{"r"} (default, pure-R) or \code{"torch"} for CI-MLP.}
\item{\code{cast_batch_args}}{Map of any other named arg accepted by
\code{\link[=cast_batch]{cast_batch()}}.}
}

All file paths are resolved relative to the directory containing the
YAML file.
}

\seealso{
\code{\link[=cast_batch]{cast_batch()}}, \code{\link[=cast_batch_resume]{cast_batch_resume()}},
\code{\link[=cast_worker_budget]{cast_worker_budget()}}, \code{\link[=cast_config_template]{cast_config_template()}}
}
