% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensemble.R
\name{cast_ensemble}
\alias{cast_ensemble}
\title{Performance-Weighted Ensemble Prediction}
\usage{
cast_ensemble(
  fit,
  cv,
  new_data,
  method = c("weighted", "best", "equal"),
  models = NULL
)
}
\arguments{
\item{fit}{A \link{cast_fit} object.}

\item{cv}{A \link{cast_cv} object providing per-model evaluation metrics.}

\item{new_data}{A \code{data.frame} with \code{lon}, \code{lat}, and environmental
variables matching the training data.}

\item{method}{Character. Ensemble strategy:
\itemize{
\item \code{"weighted"} (default): weight = Score / sum(Score), zero out
models with Score < 0.5.
\item \code{"best"}: use the single highest-scoring model.
\item \code{"equal"}: simple average of all models.
}}

\item{models}{Character vector. Subset of models to include. Default
\code{NULL} (all fitted models).}
}
\value{
A \code{cast_ensemble} object with components:
\describe{
\item{predictions}{A \code{data.frame} with \code{lon}, \code{lat}, \code{hss_ensemble},
\code{hss_sd} (cross-model standard deviation, \code{NA} for a single model),
and \code{binary_ensemble} columns.}
\item{weights}{Named numeric vector of per-model weights.}
\item{method}{The ensemble method used.}
\item{threshold}{Binary classification threshold.}
\item{model_scores}{Named numeric vector of per-model composite scores.}
}
}
\description{
Combines predictions from multiple SDM algorithms into a single
ensemble habitat suitability map. Model weights are derived from
cross-validation performance scores.
}
\details{
The composite score for each model is:

\deqn{Score = \frac{1}{3}(2 \times AUC - 1 + maxTSS + CBI)}

following the N-SDM nested-modelling framework (Adde et al. 2020).
Models with Score < 0.5 are excluded from the weighted ensemble.
Models whose predictions contain non-finite values are excluded from
the ensemble with a warning, and the remaining weights are
renormalised.
}
\references{
Adde, A., Rey, C., Brun, P., et al. (2020). N-SDM: a high-performance
computing pipeline for Nested Species Distribution Modelling.
\emph{Ecography}, 43(2), 331-334.
}
\seealso{
\code{\link[=cast_cv]{cast_cv()}}, \code{\link[=cast_predict]{cast_predict()}}, \code{\link[=cast_project]{cast_project()}}
}
