% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch-run.R
\name{cast_batch}
\alias{cast_batch}
\title{Batch Multi-Species Modeling}
\usage{
cast_batch(
  species_list,
  env_data = NULL,
  models = c("rf", "brt", "maxent", "gam"),
  train_fraction = 0.7,
  output_dir = "castSDM_batch_output",
  fig_dpi = 600L,
  parallel = TRUE,
  seed = NULL,
  verbose = TRUE,
  fit_verbose = FALSE,
  select_min_vars = 3L,
  select_num_trees = 300L,
  select_method = "cpi",
  select_max_vars = 30L,
  select_alpha = 0.05,
  select_dml_folds = 10L,
  select_cor_threshold = 0.8,
  num_threads = 1L,
  do_cv = TRUE,
  cv_k = 5L,
  cv_block_method = "grid",
  do_predict = TRUE,
  do_ensemble = TRUE,
  ensemble_method = "weighted",
  refit_full = TRUE,
  min_occ = 20L,
  esm_min = 5L,
  esm_top_k = 8L,
  esm_algo = "glm",
  response = "presence",
  prepare_env_vars = NULL,
  prepare_verbose = FALSE,
  select_verbose = FALSE,
  cv_models = NULL,
  cv_rf_ntree = 300L,
  cv_brt_n_trees = 500L,
  cv_parallel = FALSE,
  cv_verbose = FALSE,
  predict_models = NULL,
  plot_basemap = "world",
  eval_response = NULL,
  var_labels = NULL,
  dev_package_root = NULL,
  raster_stack = NULL,
  future_rasters = NULL,
  raster_mask = NULL,
  raster_compression = "LZW",
  overwrite_rasters = FALSE,
  ...
)
}
\arguments{
\item{species_list}{A named list of \code{data.frame}s.}

\item{env_data}{Optional shared \code{data.frame} for spatial prediction.}

\item{models}{Character vector. Default \code{c("rf", "brt", "maxent", "gam")}.}

\item{train_fraction}{Numeric. Default \code{0.7}.}

\item{output_dir}{Character. Default \code{"castSDM_batch_output"}.}

\item{fig_dpi}{Integer. Default \code{600}.}

\item{parallel}{Logical. Default \code{TRUE}. When \code{TRUE} and \pkg{future.apply}
is available, a \code{multisession} plan sized by \code{\link[=cast_worker_budget]{cast_worker_budget()}} is
set for the duration of the call; the previous plan and the
\code{CASTSDM_ROOT} environment variable are restored on exit.}

\item{seed}{Integer or \code{NULL}. When set, each species is modelled with a
seed derived from \code{seed} plus a stable hash of the species name, so
interrupted runs resumed with \code{\link[=cast_batch_resume]{cast_batch_resume()}} are identical to an
uninterrupted run.}

\item{verbose}{Logical. Default \code{TRUE}.}

\item{fit_verbose}{Logical. Default \code{FALSE}.}

\item{select_min_vars}{Integer. Default \code{3}.}

\item{select_num_trees}{Integer. Default \code{300}.}

\item{select_method}{Character. Variable screening method passed to
\code{\link[=cast_select]{cast_select()}}. Default \code{"cpi"}.}

\item{select_max_vars}{Candidate ceiling for DML testing / RF output.
Default \code{30}.}

\item{select_alpha}{Numeric. FDR level for the DML selector. Default \code{0.05}.}

\item{select_dml_folds}{Integer. Cross-fitting folds for the CPI/DML
selectors. Default \code{5}.}

\item{select_cor_threshold}{Numeric. Redundant-proxy correlation threshold.
Default \code{0.8}.}

\item{num_threads}{Integer. Threads for the ranger learners/benchmark.
Default \code{1}.}

\item{do_cv}{Logical. Default \code{TRUE}.}

\item{cv_k}{Integer. Default \code{5}.}

\item{cv_block_method}{Character. Default \code{"grid"}.}

\item{do_predict}{Logical. Default \code{TRUE}.}

\item{do_ensemble}{Logical. Default \code{TRUE}.}

\item{ensemble_method}{Character. \code{"weighted"}, \code{"best"}, or \code{"equal"}.
Default \code{"weighted"}.}

\item{refit_full}{Logical. Refit final models on the full data set before
spatial prediction. Default \code{TRUE}.}

\item{min_occ}{Integer. Species with fewer than this many presences are
routed to the ESM pipeline (\code{\link[=cast_esm]{cast_esm()}}) instead of the standard
route. Set \code{0} to disable the gate. Default \code{20}.}

\item{esm_min}{Integer. Species with fewer than this many presences are
skipped with a warning (no reliable model is possible). Default \code{5}.}

\item{esm_top_k}{Integer. Maximum predictors retained by the ESM route
before pair enumeration. Default \code{8}.}

\item{esm_algo}{Character. ESM base algorithm, \code{"glm"} or \code{"gam"}.
Default \code{"glm"}.}

\item{response}{Character. Default \code{"presence"}.}

\item{prepare_env_vars, prepare_verbose}{Passed to \code{\link[=cast_prepare]{cast_prepare()}}.}

\item{select_verbose}{Passed to \code{\link[=cast_select]{cast_select()}}. Default \code{FALSE}.}

\item{cv_models}{Character or \code{NULL}.}

\item{cv_rf_ntree, cv_brt_n_trees, cv_parallel, cv_verbose}{CV controls.}

\item{predict_models}{Passed to \code{\link[=cast_predict]{cast_predict()}}.}

\item{plot_basemap}{Character. Default \code{"world"}.}

\item{eval_response}{Character or \code{NULL}.}

\item{var_labels}{Named character vector or \code{NULL}.}

\item{dev_package_root}{Character or \code{NULL}.}

\item{raster_stack}{Optional \code{terra::SpatRaster} for raster-based
ensemble prediction via \code{\link[=cast_ensemble_raster]{cast_ensemble_raster()}}.}

\item{future_rasters}{Optional named list of \code{terra::SpatRaster} stacks
for future projection via \code{\link[=cast_project_raster]{cast_project_raster()}}.}

\item{raster_mask}{Optional \code{terra::SpatRaster} prediction mask.}

\item{raster_compression}{Character. Default \code{"LZW"}.}

\item{overwrite_rasters}{Logical. Default \code{FALSE}.}

\item{...}{Additional arguments forwarded to \code{\link[=cast_fit]{cast_fit()}}.}
}
\value{
A \code{cast_batch} object.
}
\description{
One-stop interface that runs the full castSDM pipeline on multiple
species, optionally in parallel. Results (RDS + diagnostic figures) are
saved to separate subdirectories per species.
}
\seealso{
\code{\link[=cast]{cast()}}, \code{\link[=cast_fit]{cast_fit()}}, \code{\link[=cast_select]{cast_select()}}, \code{\link[=cast_cv]{cast_cv()}}
}
