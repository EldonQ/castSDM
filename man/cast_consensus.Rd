% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consensus-select.R
\name{cast_consensus}
\alias{cast_consensus}
\title{Consensus Selection Across Spatial Folds}
\usage{
cast_consensus(cv, threshold = 0.5)
}
\arguments{
\item{cv}{A \code{cast_cv} object produced with a non-\code{NULL} \code{select_method}.}

\item{threshold}{Minimum fold-selection frequency (in \verb{[0, 1]}) for
membership in the consensus set. Default \code{0.5} (majority of folds).}
}
\value{
A \code{cast_select} object whose \code{selected} members form the
consensus set and whose \code{scores} data.frame holds the per-predictor
fold frequency (\code{variable}, \code{freq}, \code{selected}).
}
\description{
Aggregates the fold-specific selections stored in a \code{\link[=cast_cv]{cast_cv()}} object
into a selection-frequency table and a consensus variable set: predictors
retained in at least \code{threshold} (a proportion) of the outer folds.
}
\details{
Nested selection is re-run in every spatial fold, so a predictor selected
in only one fold is likely a region-specific (or noise) association; a
predictor selected in most folds is a candidate driver that transfers
across regions. The frequency table makes that trade-off visible and the
consensus set operationalises it.
}
\seealso{
\code{\link[=cast_cv]{cast_cv()}}, \code{\link[=cast_fit]{cast_fit()}}
}
