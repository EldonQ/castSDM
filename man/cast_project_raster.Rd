% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project-future.R
\name{cast_project_raster}
\alias{cast_project_raster}
\title{Raster-Based Future Climate Projection}
\usage{
cast_project_raster(
  fit,
  cv,
  current_raster,
  future_rasters,
  output_dir,
  method = c("weighted", "best", "equal"),
  models = NULL,
  mask = NULL,
  overwrite = FALSE,
  compression = "LZW",
  clamp = NULL,
  verbose = TRUE
)
}
\arguments{
\item{fit}{A \link{cast_fit} object.}

\item{cv}{A \link{cast_cv} object for computing ensemble weights and
thresholds.}

\item{current_raster}{A \code{terra::SpatRaster} stack of current
environmental variables (layer names must match \code{fit$env_vars}).}

\item{future_rasters}{A named list of \code{terra::SpatRaster} stacks,
each for a future scenario (e.g., \code{list(ssp126_2050 = rast1, ...)}).}

\item{output_dir}{Character. Output directory for all rasters and CSV.}

\item{method}{Character. Ensemble method. Default \code{"weighted"}.}

\item{models}{Character vector or \code{NULL}. Models to use.}

\item{mask}{A \code{terra::SpatRaster} or \code{NULL}. Prediction mask.}

\item{overwrite}{Logical. Overwrite existing outputs. Default \code{FALSE}.}

\item{compression}{Character. GeoTIFF compression. Default \code{"LZW"}.}

\item{clamp}{Reserved for extrapolation control: forwarded to
\code{\link[=cast_ensemble_raster]{cast_ensemble_raster()}} when the installed version supports a \code{clamp}
argument; otherwise ignored with a warning. Default \code{NULL}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
A list with components:
\describe{
\item{current}{List with \code{hss_path}, \code{binary_path}, etc.}
\item{future}{Named list of per-scenario results.}
\item{stats}{A \code{data.frame} with per-scenario statistics.}
\item{output_dir}{The output directory path.}
}
}
\description{
Projects species distribution under multiple future climate scenarios
using raster inputs and the ensemble model. Generates HSS rasters,
binary rasters, change-class rasters, and summary statistics for each
scenario. This is the raster-native equivalent of \code{\link[=cast_project]{cast_project()}}.
}
\details{
For each scenario, a change-class raster is computed:
\itemize{
\item \code{1} = gain (absent now, present in future)
\item \code{-1} = loss (present now, absent in future)
\item \code{2} = stable present
\item \code{0} = stable absent
}

A scenario that fails is skipped with a warning and recorded as an \code{NA}
row in the statistics table; remaining scenarios continue.

Centroid shift is computed as the HSS-weighted great-circle distance.
}
\seealso{
\code{\link[=cast_ensemble_raster]{cast_ensemble_raster()}}, \code{\link[=cast_project]{cast_project()}}
}
