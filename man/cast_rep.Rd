% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replicates.R
\name{cast_rep}
\alias{cast_rep}
\title{Replicated Background-Sampling Pipeline}
\usage{
cast_rep(
  occurrences,
  raster_stack,
  env_data = NULL,
  n_reps = 10,
  models = c("rf"),
  select_method = "cpi",
  seed = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{occurrences}{A \code{data.frame} with \code{lon}, \code{lat} columns (presence
locations). May carry extra columns, which are preserved.}

\item{raster_stack}{A \code{terra::SpatRaster} of environmental variables
used for background sampling and value extraction.}

\item{env_data}{Optional prediction grid (\code{lon}, \code{lat} + predictors). When
supplied, per-replicate predictions are stored and aggregated into
\code{prediction} (mean + SD per cell).}

\item{n_reps}{Number of background replicates. Default \code{10}.}

\item{models}{Models passed to \code{\link[=cast]{cast()}}. Default \code{c("rf")}.}

\item{select_method}{Selection method passed to \code{\link[=cast]{cast()}}. Default \code{"cpi"}.}

\item{seed}{Integer or \code{NULL}. Replicate \code{r} uses \code{seed + r}.}

\item{verbose}{Logical. Default \code{TRUE}.}

\item{...}{Further arguments passed to \code{\link[=cast]{cast()}} (e.g. \code{do_cv}, \code{cv_k},
\code{select_min_vars}), and \code{background} arguments passed to
\code{\link[=cast_background]{cast_background()}} (\code{ratio}, \code{min_bg}, \code{max_bg}, \code{strategy},
\code{cell_thin}, \code{exclude_presence}).}
}
\value{
A \code{cast_rep} object with components:
\describe{
\item{reps}{List of per-replicate \code{\link[=cast]{cast()}} results.}
\item{metrics}{Aggregated evaluation metrics (mean and SD across
replicates) per model.}
\item{selection_freq}{Data.frame with each predictor's selection
frequency across replicates.}
\item{prediction}{\code{data.frame} with \code{lon}, \code{lat}, and per-model
\verb{hss_mean_<model>} / \verb{hss_sd_<model>} columns aggregated from the
replicate predictions (only when \code{env_data} is supplied). The
aggregation is per model: each fitted model gets its own mean/SD
column pair, and replicates that lack a prediction surface are
dropped replicate-wise instead of voiding the whole aggregation.}
\item{n_reps}{Number of completed replicates.}
}
}
\description{
Runs \code{\link[=cast_background]{cast_background()}} + \code{\link[=cast]{cast()}} for \code{n_reps} independent background
draws and aggregates the results. Replicates quantify the sensitivity of
evaluation metrics, variable selection, and predicted suitability to the
arbitrary choice of pseudo-absence locations - the dominant source of
run-to-run variance in presence-background SDMs.
}
\references{
Barbet-Massin, M. et al. (2012). Selecting pseudo-absences for species
distribution models: how, where and how many? \emph{Methods in Ecology and
Evolution}, 3(2), 327-338.
}
\seealso{
\code{\link[=cast_background]{cast_background()}}, \code{\link[=cast]{cast()}}, \code{\link[=plot.cast_rep]{plot.cast_rep()}}
}
