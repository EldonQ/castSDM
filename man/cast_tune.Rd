% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune.R
\name{cast_tune}
\alias{cast_tune}
\title{Unified Per-Algorithm Hyperparameter Grid Search}
\usage{
cast_tune(
  data,
  models = c("rf", "brt"),
  screen = NULL,
  dag = NULL,
  grid = NULL,
  n_splits = 30L,
  val_fraction = 0.25,
  response = "presence",
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{Training \code{data.frame}. Must contain \code{response}.}

\item{models}{Character vector. Algorithms to tune. Default
\code{c("rf", "brt")}.}

\item{screen, dag}{As in \code{\link[=cast_fit]{cast_fit()}}.}

\item{grid}{Optional named list of per-model parameter grids. Each
element is a \code{data.frame} (or list-of-vectors that gets passed to
\code{expand.grid}). If \code{NULL}, uses \code{\link[=cast_default_grid]{cast_default_grid()}}.}

\item{n_splits}{Integer. Random splits per grid point. Default \code{30L}
(raise toward \code{100} for paper-grade tuning).}

\item{val_fraction}{Numeric. Validation hold-out per split. Default
\code{0.25}.}

\item{response}{Character. Default \code{"presence"}.}

\item{seed}{Integer or \code{NULL}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
A \code{cast_tune} object: list of best parameters per model plus
the full grid scores.
}
\description{
Runs split-sample evaluation over a user-supplied (or default) grid for
each requested algorithm and returns a \code{cast_tune} object that
\code{\link[=cast_fit]{cast_fit()}} consumes via its \code{tune_result} argument. Selection is
scored on \code{AUC + TSS - 1} (clamped to \verb{[0, 2]}) averaged across splits;
ties broken by AUC.
}
\details{
Supported algorithms (\code{models}):
\itemize{
\item \code{"rf"}: tunes \code{rf_ntree}, plus internally \code{mtry} defaults via
\code{ranger}.
\item \code{"brt"}: tunes \code{brt_n_trees}, \code{brt_depth}, learning rate.
\item \code{"gam"}: no hyper-grid (single fit, included for completeness).
\item \code{"maxent"}: no hyper-grid via \code{maxnet} defaults.
}

Each grid point is evaluated on \code{n_splits} random train/validation
splits of \code{val_fraction} size. Evaluation uses the same internal AUC
\itemize{
\item TSS scorer as \code{\link[=cast_evaluate]{cast_evaluate()}}, skipping CBI to keep tuning fast.
}
}
\seealso{
\code{\link[=cast_fit]{cast_fit()}}, \code{\link[=cast_default_grid]{cast_default_grid()}}
}
