% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlp-pureR.R
\name{cast_mlp_fit}
\alias{cast_mlp_fit}
\title{Fit a Pure-R Residual MLP (CPU-Native CI-MLP Backend)}
\usage{
cast_mlp_fit(
  X,
  y,
  hidden = 64L,
  dropout = 0.2,
  epochs = 300L,
  batch_size = 64L,
  lr = 0.001,
  weight_decay = 1e-04,
  warmup_epochs = 10L,
  patience = 40L,
  val_idx = NULL,
  val_fraction = 0.2,
  focal_alpha = 0.25,
  focal_gamma = 2,
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{X}{Numeric matrix or data.frame. Predictor matrix (already scaled).}

\item{y}{Numeric vector of 0/1 binary labels.}

\item{hidden}{Integer. Hidden width. Default \code{64}.}

\item{dropout}{Numeric in \verb{[0, 1)}. Default \code{0.2}.}

\item{epochs}{Integer. Maximum epochs. Default \code{300}.}

\item{batch_size}{Integer. Mini-batch size. Default \code{64}.}

\item{lr}{Numeric. Peak learning rate. Default \code{1e-3}.}

\item{weight_decay}{Numeric. AdamW weight decay. Default \code{1e-4}.}

\item{warmup_epochs}{Integer. Linear warmup length. Default \code{10}.}

\item{patience}{Integer. Early-stopping patience (epochs without
validation AUC improvement). Default \code{40}.}

\item{val_idx}{Integer vector. Indices of validation rows. If \code{NULL},
a stratified 20\% split is built internally.}

\item{val_fraction}{Numeric. Used when \code{val_idx = NULL}. Default \code{0.2}.}

\item{focal_alpha, focal_gamma}{Numeric. Focal loss parameters.}

\item{seed}{Integer. Random seed for reproducibility.}

\item{verbose}{Logical. Print per-epoch progress every 25 epochs.}
}
\value{
A list with class \code{"cast_mlp"} containing fitted weights, the
training history, and the configuration. Compatible with
\code{\link[=cast_mlp_predict]{cast_mlp_predict()}}.
}
\description{
A torch-free, dependency-free implementation of the residual MLP used by
\code{\link[=cast_fit]{cast_fit()}} when \code{backend = "r"} (the default). Mirrors the architecture
of the legacy torch backend: input projection + LayerNorm + SiLU,
three residual blocks (pre-norm, two linear-SiLU layers, dropout),
bottleneck + head. Trained with AdamW + warmup + cosine LR schedule and
focal loss for class imbalance.
}
\seealso{
\code{\link[=cast_mlp_predict]{cast_mlp_predict()}}, \code{\link[=cast_fit]{cast_fit()}}
}
