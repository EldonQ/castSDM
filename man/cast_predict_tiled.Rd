% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict-tiled.R
\name{cast_predict_tiled}
\alias{cast_predict_tiled}
\title{Tile-based Spatial Prediction for Large Rasters}
\usage{
cast_predict_tiled(
  fit,
  raster,
  output_dir,
  tile_size = 512L,
  models = NULL,
  overwrite = FALSE,
  compression = "LZW",
  verbose = TRUE
)
}
\arguments{
\item{fit}{A \link{cast_fit} object.}

\item{raster}{A \code{terra::SpatRaster} whose layer names cover the
environmental variables in \code{fit$env_vars}. May be a path passed to
\code{terra::rast()}.}

\item{output_dir}{Directory in which per-model TIFFs and tile
checkpoints are written. Created if it does not exist.}

\item{tile_size}{Integer of length 1 or 2. Tile dimensions in pixels
(height x width). Default \code{512}.}

\item{models}{Character vector. Which fitted models to predict. Default
all.}

\item{overwrite}{Logical. Overwrite existing output rasters. Default
\code{FALSE} - existing files trigger a \code{cli_inform()} and skip.}

\item{compression}{Character. GeoTIFF compression. Default \code{"LZW"}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
A list of class \code{cast_predict_tiled} with components:
\describe{
\item{rasters}{Named list of file paths, one per model.}
\item{n_tiles}{Number of tiles processed.}
\item{models}{Character vector of model names.}
}
}
\description{
Memory-bounded variant of \code{\link[=cast_predict]{cast_predict()}} for prediction grids whose
full pixel count would otherwise exceed available RAM. Reads the
covariate raster stack tile by tile, applies a fitted model, and writes
the per-model habitat-suitability surface (HSS) to disk via
\code{terra::writeRaster()}.
}
\details{
Per-tile checkpoints are written eagerly under a per-fit directory
(\verb{<output_dir>/.cast_tiles_<fingerprint>/}) so an interrupted run can be
resumed without recomputing finished tiles, and a new fit can never
replay tiles predicted by an older model. \code{overwrite = TRUE} bypasses
and refreshes the checkpoints.

Cells with missing (\code{NA}) covariates stay \code{NA} in the output rasters -
they are recorded before \code{\link[=cast_predict]{cast_predict()}} imputes them and written back
as \code{NA} afterwards, matching the NA semantics of
\code{\link[=cast_ensemble_raster]{cast_ensemble_raster()}}. No suitability values are fabricated outside
the data mask (e.g. ocean pixels).
}
\seealso{
\code{\link[=cast_predict]{cast_predict()}}
}
