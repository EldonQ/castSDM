% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bioclim-io.R
\name{cast_load_bioclim}
\alias{cast_load_bioclim}
\title{Load CHNECO26 Bioclim Raster Stack}
\usage{
cast_load_bioclim(
  bioclim_root,
  variables = sprintf("bio\%02d", 1:19),
  period = "present",
  sub_period = "1991_2020",
  scenario = NULL,
  model = "ensmean",
  metadata_csv = NULL,
  verbose = TRUE
)
}
\arguments{
\item{bioclim_root}{Character. Root directory of the bioclim dataset
(e.g., \code{"E:/CHECO26/Bioclim/CHNECO26_Bioclim"}).}

\item{variables}{Character vector. Variable names to load
(e.g., \code{sprintf("bio\%02d", 1:19)}). Default loads standard 19 bioclim.}

\item{period}{Character. \code{"present"} or \code{"future"}. Default \code{"present"}.}

\item{sub_period}{Character. Time period (e.g., \code{"1991_2020"},
\code{"2021_2040"}). Default \code{"1991_2020"}.}

\item{scenario}{Character or \code{NULL}. SSP scenario for future periods
(e.g., \code{"ssp126"}). Required when \code{period = "future"}.}

\item{model}{Character or \code{NULL}. Climate model (e.g., \code{"ensmean"},
\code{"access_cm2"}). For future periods. Default \code{"ensmean"}.}

\item{metadata_csv}{Character or \code{NULL}. Path to the CHNECO26 metadata
CSV. If \code{NULL}, attempts to find it at
\verb{<bioclim_root>/CHNECO26_datalayers_details_bioclim.csv}.
If the CSV doesn't exist, falls back to recursive directory scanning.}

\item{verbose}{Logical. Print progress. Default \code{TRUE}.}
}
\value{
A named \code{terra::SpatRaster} stack with one layer per variable.
Layer names match the \code{variables} argument.
}
\description{
Locates and loads bioclimatic raster layers from a CHNECO26-style directory
structure where each variable lives in its own subdirectory. Supports two
discovery modes: metadata CSV (preferred) or recursive directory scan.
}
\details{
\strong{Metadata CSV mode} (preferred): Reads the CSV, filters by period,
sub_period, scenario, model, and variable, then constructs absolute
paths from relative_path + bioclim_root.

\strong{Recursive scan mode} (fallback): Searches recursively for TIF files
matching the variable names, excluding \code{.tmp.tif} files.
}
\section{CHNECO26 Directory Layout}{


\if{html}{\out{<div class="sourceCode">}}\preformatted{bioclim_root/
  CHNECO26_datalayers_details_bioclim.csv   (metadata)
  bioclim/cnclim1km/present/1991_2020/
    bio01/bioclim_cnclim1km_present_1991_2020_bio01.tif
    bio02/bioclim_cnclim1km_present_1991_2020_bio02.tif
    ...
  bioclim/cnclim1km_ensmean/future/
    2021_2040/ssp126/bio01/...tif
    ...
}\if{html}{\out{</div>}}
}

