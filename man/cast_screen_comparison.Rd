% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/var-select.R
\name{cast_screen_comparison}
\alias{cast_screen_comparison}
\title{Compare castSDM Conditional Screening Against Conventional Baselines}
\usage{
cast_screen_comparison(
  data,
  response = "presence",
  cpi = NULL,
  pool = 40L,
  cor_threshold = 0.7,
  vif_threshold = 10,
  alpha = 0.05,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{Data frame with response, coordinates, and predictors.}

\item{response}{Binary response column. Default \code{"presence"}.}

\item{cpi}{Optional precomputed \code{cast_select} object (\code{method = "cpi"}) to
reuse instead of recomputing the conditional screen.}

\item{pool}{Integer. Size of the shared candidate pool. Default \code{40}.}

\item{cor_threshold}{Correlation-filter cutoff. Default \code{0.7}.}

\item{vif_threshold}{Stepwise-VIF cutoff. Default \code{10}.}

\item{alpha}{FDR level for the marginal/associational screens and CPI.
Default \code{0.05}.}

\item{seed}{Random seed.}

\item{verbose}{Print progress.}
}
\value{
A \code{cast_screen_comparison} object.
}
\description{
Runs the predictor-selection strategies most researchers actually use --
a correlation filter (Dormann et al. 2013), stepwise VIF (Naimi & Araujo
2016), a univariate marginal screen (Guisan & Zimmermann 2000), and
random-forest permutation importance (Cutler et al. 2007) -- alongside the
castSDM conditional predictive impact screen, on the \emph{same} data. The
associational baselines cannot separate a genuine driver from a collinear
bystander, whereas CPI tests each predictor given all others; the returned
object makes that contrast explicit for the Results narrative.
}
\details{
Because stepwise VIF and pairwise correlation scale poorly, the predictor
pool is first reduced to the \code{pool} most marginally associated predictors
(this mirrors practice -- nobody runs VIF on hundreds of raw layers) and
every method, including CPI, is compared on that shared pool.
}
\seealso{
\code{\link[=cast_select]{cast_select()}}, \code{\link[=plot.cast_screen_comparison]{plot.cast_screen_comparison()}}
}
