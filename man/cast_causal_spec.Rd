% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal-prior-select.R
\name{cast_causal_spec}
\alias{cast_causal_spec}
\title{Build an Auditable Causal-Role Specification}
\usage{
cast_causal_spec(
  variables,
  template = c("habitat", "upstream"),
  overrides = NULL
)
}
\arguments{
\item{variables}{Character vector of predictor names.}

\item{template}{Character. Currently \code{"habitat"} (the validated default) or
\code{"upstream"} (vegetation and land-cover are treated as mediators).}

\item{overrides}{Optional data frame containing \code{variable} and any of
\code{role}, \code{confidence}, \code{rationale}, or \code{parent}.}
}
\value{
A data frame with one auditable role declaration per variable.
}
\description{
Creates a conservative role table for environmental predictors. Roles are
scientific assumptions used to constrain selection; they are not causal
directions learned from occurrence records. The default habitat template
treats climate, soil, topography, hydrology, vegetation and land-cover
variables as candidate ecological drivers, excludes anthropogenic sampling
proxies, and fails unrecognised variables closed to \code{"unknown"}.
}
