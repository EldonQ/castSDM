% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cast.R
\name{cast}
\alias{cast}
\title{Run the Full castSDM Pipeline}
\usage{
cast(
  species_data,
  env_data = NULL,
  models = c("rf", "brt", "maxent", "gam"),
  train_fraction = 0.7,
  select_min_vars = 0L,
  select_method = "cpi",
  select_num_trees = 300L,
  select_max_vars = NULL,
  select_alpha = 0.05,
  select_n_folds = 10L,
  select_cor_threshold = 0.8,
  num_threads = 1L,
  do_cv = TRUE,
  cv_k = 5L,
  cv_block_method = "grid",
  do_predict = NULL,
  do_ensemble = TRUE,
  ensemble_method = "weighted",
  refit_full = TRUE,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{species_data}{A \code{data.frame} with columns: \code{lon}, \code{lat}, \code{presence}
(0/1), and environmental variables.}

\item{env_data}{Optional \code{data.frame} of environmental variables for the
full spatial grid (used for prediction). Must contain \code{lon}, \code{lat}, and
the same environmental columns as \code{species_data}.}

\item{models}{Character vector of models to fit. Options: \code{"rf"},
\code{"maxent"}, \code{"brt"}, \code{"gam"}. Default \code{c("rf", "brt", "maxent", "gam")}.}

\item{train_fraction}{Numeric. Fraction of data for training. Default \code{0.7}.}

\item{select_min_vars}{Integer. Minimum retained variables. Default \code{0}
(an empty selection is allowed).}

\item{select_method}{Character. Variable screening method passed to
\code{\link[=cast_select]{cast_select()}}. Default \code{"cpi"}.}

\item{select_num_trees}{Integer. Trees in the RF nuisance/benchmark forests.
Default \code{300}.}

\item{select_max_vars}{Optional candidate ceiling for the CPI selector / RF
output. \code{NULL} tests every predictor; a positive integer pre-screens by RF
importance first. Default \code{NULL}.}

\item{select_alpha}{Numeric. FDR level for the CPI selector. Default \code{0.05}.}

\item{select_n_folds}{Integer. Cross-fitting folds for the CPI selector.
Default \code{10}; fold-level CPI inference needs enough folds for its t-test
(df = folds - 1).}

\item{select_cor_threshold}{Numeric. Absolute correlation threshold for the
RF benchmark. Default \code{0.8}.}

\item{num_threads}{Integer. Threads for the ranger learners/benchmark.
Default \code{1}.}

\item{do_cv}{Logical. Run spatial cross-validation. Default \code{TRUE}.}

\item{cv_k}{Integer. Number of spatial folds. Default \code{5}.}

\item{cv_block_method}{Character. Spatial blocking strategy. Default
\code{"grid"} - grid cells grouped into spatially contiguous folds (the
legacy interleaved packing is still available as \code{"grid_random"}; see
\code{\link[=cast_cv]{cast_cv()}}, which also offers a \code{buffer} exclusion band).}

\item{do_predict}{Logical. Generate spatial predictions. Default \code{TRUE}
if \code{env_data} is provided.}

\item{do_ensemble}{Logical. Generate ensemble prediction. Default \code{TRUE}.}

\item{ensemble_method}{Character. Ensemble method: \code{"weighted"},
\code{"best"}, \code{"equal"}. Default \code{"weighted"}.}

\item{refit_full}{Logical. Refit the final models on the full data set
(instead of the training split only) before spatial prediction and
ensemble, standard SDM practice. Variable selection is still decided on
the training split. Default \code{TRUE}.}

\item{seed}{Integer or \code{NULL}.}

\item{verbose}{Logical. Print progress. Default \code{TRUE}.}
}
\value{
A \code{cast_result} object (S3 class) containing pipeline outputs.
Use \code{\link[=print]{print()}}, \code{\link[=summary]{summary()}}, \code{\link[=plot]{plot()}}.
}
\description{
One-step pipeline that executes the entire workflow: data splitting,
variable screening, model fitting, spatial cross-validation,
evaluation, ensemble prediction, and future projection.
}
\seealso{
\code{\link[=cast_select]{cast_select()}}, \code{\link[=cast_fit]{cast_fit()}}, \code{\link[=cast_ensemble]{cast_ensemble()}},
\code{\link[=cast_predict]{cast_predict()}}
}
