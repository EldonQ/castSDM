% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/climate-data.R
\name{cast_prepare_future_env}
\alias{cast_prepare_future_env}
\title{Prepare Future Environment Data for Projection}
\usage{
cast_prepare_future_env(
  rasters,
  coords,
  static_vars = NULL,
  var_mapping = NULL,
  save_dir = NULL,
  verbose = TRUE
)
}
\arguments{
\item{rasters}{A named list of \code{SpatRaster} objects (from
\code{\link[=cast_download_cmip6]{cast_download_cmip6()}} or user-supplied), or a single \code{SpatRaster}.
When a named list, each element becomes one future scenario.}

\item{coords}{A \code{data.frame} with \code{lon} and \code{lat} columns defining the
prediction grid (same grid used for current predictions).}

\item{static_vars}{Optional \code{data.frame} with the same number of rows as
\code{coords}, containing non-climate variables (elevation, land cover, etc.)
that are assumed constant across future scenarios. Default \code{NULL}.}

\item{var_mapping}{Optional named character vector mapping raster layer
names to training data variable names. For example:
\code{c("wc2.1_2.5m_bioc_01" = "bio01", "wc2.1_2.5m_bioc_02" = "bio02")}.
When \code{NULL} (default), attempts automatic matching by extracting
trailing bioclim numbers (e.g., \code{"_01"} -> \code{"bio01"}).}

\item{save_dir}{Optional path. When provided, saves each future
environment data.frame as a CSV file. Default \code{NULL}.}

\item{verbose}{Logical. Print progress. Default \code{TRUE}.}
}
\value{
A named list of \code{data.frame}s, each containing \code{lon}, \code{lat}, and
environmental variables ready for \code{\link[=cast_project]{cast_project()}}.
}
\description{
Extracts environmental values at species distribution grid points from
raster data, joins with static (non-climate) variables, and renames
columns to match training data variable names.
}
\details{
\subsection{Variable name mapping}{

CMIP6 raster layers from \code{geodata} have long names like
\code{"wc2.1_2.5m_bioc_ACCESS-CM2_ssp245_2041-2060_01"}. The \code{var_mapping}
parameter or the auto-detect logic strips these to match your training
data column names (e.g., \code{"bio01"}).
}

\subsection{Static variables}{

Topographic variables (elevation, slope, terrain ruggedness) and land
cover are typically assumed constant in future projections. Pass them
via \code{static_vars} — they are joined by row position (must match
\code{coords} row count).
}
}
\seealso{
\code{\link[=cast_download_cmip6]{cast_download_cmip6()}}, \code{\link[=cast_project]{cast_project()}}
}
