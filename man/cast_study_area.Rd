% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/study-area.R
\name{cast_study_area}
\alias{cast_study_area}
\title{Define Species-Specific Study Area (Accessible Area M)}
\usage{
cast_study_area(
  occurrences,
  raster_template,
  method = c("buffer", "bbox_buffer", "full"),
  buffer_km = 300,
  min_cells = 1000L,
  verbose = TRUE
)
}
\arguments{
\item{occurrences}{A \code{data.frame} with at least \code{lon} and \code{lat} columns,
or an \code{sf} points object.}

\item{raster_template}{A \code{terra::SpatRaster} used as the reference grid
(e.g., one layer of the environmental stack). Only cells that are
non-NA in this template can be part of the study area.}

\item{method}{Character. How to define the study area:
\itemize{
\item \code{"buffer"} (default): convex hull around occurrences + buffer.
\item \code{"bbox_buffer"}: bounding box of occurrences + buffer (simpler shape).
\item \code{"full"}: entire non-NA extent of \code{raster_template} (no restriction).
}}

\item{buffer_km}{Numeric. Buffer distance in kilometers around occurrences.
Only used when \code{method} is \code{"buffer"} or \code{"bbox_buffer"}. Default \code{300}.}

\item{min_cells}{Integer. Minimum number of valid cells required in the
study area. If the result has fewer cells, the buffer is automatically
expanded. Default \code{1000L}.}

\item{verbose}{Logical. Print informational messages. Default \code{TRUE}.}
}
\value{
A \code{cast_study_area} object (S3 class) with components:
\describe{
\item{mask}{A \code{terra::SpatRaster} (0/1 or NA) at the same resolution and
CRS as \code{raster_template}.}
\item{boundary}{An \code{sf} polygon of the study area boundary.}
\item{n_cells}{Integer. Number of valid (non-NA) cells in the mask.}
\item{method}{Character. The method used.}
\item{buffer_km}{Numeric. The buffer distance used (NA for \code{"full"}).}
\item{n_occurrences}{Integer. Number of occurrence points provided.}
}
}
\description{
Creates a study area mask following the BAM framework (Barve et al. 2011),
which recommends using the species' accessible area (M) rather than
administrative boundaries. The study area constrains both background
point sampling and spatial prediction extent.
}
\references{
Barve, N., Barve, V., Jimenez-Valverde, A. et al. (2011). The crucial
role of the accessible area in ecological niche modeling and species
distribution modeling. \emph{Ecological Modelling}, 222(11), 1810-1819.
}
\seealso{
\code{\link[=cast_background]{cast_background()}}, \code{\link[=cast_ensemble_raster]{cast_ensemble_raster()}}
}
