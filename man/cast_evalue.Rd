% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity-evalue.R
\name{cast_evalue}
\alias{cast_evalue}
\title{Sensitivity Analysis via E-values}
\usage{
cast_evalue(ate, transform = c("RR", "OR"), p0 = 0.5, verbose = TRUE)
}
\arguments{
\item{ate}{A \link{cast_ate} object.}

\item{transform}{Character. How to convert ATE coefficients (risk
differences on a 0-1 outcome) to approximate risk ratios.
\describe{
\item{\code{"RR"} (default)}{Use \eqn{RR \approx 1 + |ATE| / p_0} where
\eqn{p_0 = 0.5} is assumed baseline prevalence. Appropriate when ATE
represents a difference in proportions.}
\item{\code{"OR"}}{Convert via \eqn{OR = (p_0 + ATE) / p_0 \cdot
      (1-p_0) / (1-p_0-ATE)}, then approximate RR from OR using the
square-root bound: \eqn{RR \approx \sqrt{OR}}.}
}}

\item{p0}{Numeric. Assumed baseline outcome prevalence for RR/OR
approximation. Default \code{0.5}.}

\item{verbose}{Logical. Print summary. Default \code{TRUE}.}
}
\value{
A \code{cast_ate} object with the \code{estimates} data.frame augmented by
columns \code{rr} (approximate risk ratio), \code{evalue_point} (E-value for the
point estimate), and \code{evalue_ci} (E-value for the confidence interval
bound closest to the null).
}
\description{
Computes E-values for each ATE estimate, quantifying the minimum strength
of unmeasured confounding (on the risk ratio scale) needed to explain away
the observed causal effect. Larger E-values indicate more robust findings.
}
\details{
The E-value for a risk ratio \eqn{RR \geq 1} is:
\deqn{E = RR + \sqrt{RR \times (RR - 1)}}
For the confidence interval bound, the same formula is applied to the
lower CI bound's RR; if the CI includes the null (RR = 1), the E-value
is 1.
}
\references{
VanderWeele, T.J. and Ding, P. (2017). Sensitivity analysis in
observational research: Introducing the E-value.
\emph{Annals of Internal Medicine}, 167(4), 268-274.
}
\seealso{
\code{\link[=cast_ate]{cast_ate()}}
}
