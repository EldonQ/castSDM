% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/china-map.R
\name{cast_plot_china}
\alias{cast_plot_china}
\title{Render a Raster as a Standards-Compliant China Map}
\usage{
cast_plot_china(
  raster,
  output,
  title = "",
  legend = "",
  palette = "viridis",
  midpoint = NA_real_,
  clamp = FALSE,
  reverse = FALSE,
  width = 7,
  height = 6.2,
  dpi = 600
)
}
\arguments{
\item{raster}{A single-layer \code{SpatRaster}.}

\item{output}{Output path without extension (writes \code{.png} and \code{.pdf}).}

\item{title}{Map title. Default empty.}

\item{legend}{Legend title. Default empty.}

\item{palette}{Palette name: sequential themes (\code{blues}, \code{greens},
\code{teal}, \code{mako}, \code{rocket}, ...), diverging themes (\code{rdbu}, \code{rdylbu},
\code{spectral}, \code{brbg}, \code{puor}, \code{prgn}; pair with \code{midpoint}), perceptual
(\code{viridis}, \code{cividis}, \code{inferno}, \code{plasma}), or \code{hillshade}.}

\item{midpoint}{Diverging midpoint. Default \code{NA} (no mid-rescaling).}

\item{clamp}{Logical. Clamp colour limits to the 2-98th percentiles.}

\item{reverse}{Reverse the palette.}

\item{width, height, dpi}{Figure size and resolution.}
}
\value{
Invisibly, the output path prefix (files are written).
}
\description{
Wraps the geo-viz engine: China boundary + South China Sea nine-dash line
\itemize{
\item inset, Nature-grade colour-blind-friendly palettes, transparent
background, 600 dpi PNG and cairo PDF export. The input raster is
reprojected to a unified WGS84 display grid with GDAL (not terra, which
segfaults on some machines) and masked to China.
}
}
