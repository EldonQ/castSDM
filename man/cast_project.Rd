% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project-future.R
\name{cast_project}
\alias{cast_project}
\title{Project Species Distribution Under Future Climate Scenarios}
\usage{
cast_project(
  fit,
  cv,
  current_env,
  future_envs,
  method = c("weighted", "best", "equal"),
  threshold_method = "maxTSS",
  models = NULL,
  save_dir = NULL
)
}
\arguments{
\item{fit}{A \link{cast_fit} object.}

\item{cv}{A \link{cast_cv} object for computing ensemble weights and
thresholds.}

\item{current_env}{A \code{data.frame} with \code{lon}, \code{lat}, and environmental
variables for the current (baseline) period.}

\item{future_envs}{A named list of \code{data.frame}s, each with the same
\code{lon}, \code{lat} grid and environmental variables for a future scenario
(e.g., \code{list(ssp245_2070 = df1, ssp585_2070 = df2)}).}

\item{method}{Character. Ensemble strategy: \code{"weighted"} (default),
\code{"best"}, \code{"equal"}. See \code{\link[=cast_ensemble]{cast_ensemble()}}.}

\item{threshold_method}{Character. Binary threshold method. Default
\code{"maxTSS"}.}

\item{models}{Character vector. Models to use. Default \code{NULL} (all).}

\item{save_dir}{Optional character. When provided, saves CSV and (if
\code{terra} is available) GeoTIFF outputs to this directory:
\itemize{
\item \code{current_prediction.csv} / \code{.tif}
\item \verb{\{scenario\}_prediction.csv} / \code{.tif}
\item \verb{\{scenario\}_change.csv} / \code{.tif}
\item \code{projection_stats.csv}
Default \code{NULL} (no file output).
}}
}
\value{
A \code{cast_project} object with components:
\describe{
\item{current}{A \code{cast_ensemble} object for the current climate.}
\item{future}{Named list of \code{cast_ensemble} objects for each scenario.}
\item{changes}{Named list of change \code{data.frame}s with \code{lon}, \code{lat},
\code{change} columns.}
\item{stats}{A \code{data.frame} with columns \code{scenario}, \code{n_gain},
\code{n_loss}, \code{n_stable_present}, \code{n_stable_absent}, \code{pct_change},
\code{centroid_shift_km}.}
}
}
\description{
Applies the same ensemble model(s) and weights from current-climate
predictions to one or more future climate scenarios, then computes
range change statistics (gain, loss, stable).
}
\details{
Change categories per grid cell:
\itemize{
\item \strong{gain}: absent now, present under future scenario.
\item \strong{loss}: present now, absent under future scenario.
\item \strong{stable_present}: present in both.
\item \strong{stable_absent}: absent in both.
}

Centroid shift is computed as the great-circle distance between the
weighted centroid of current presence and the weighted centroid of
future presence.
}
\seealso{
\code{\link[=cast_ensemble]{cast_ensemble()}}, \code{\link[=cast_fit]{cast_fit()}}, \code{\link[=cast_cv]{cast_cv()}}
}
