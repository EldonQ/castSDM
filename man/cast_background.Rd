% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/background-sample.R
\name{cast_background}
\alias{cast_background}
\title{Generate Adaptive Background (Pseudo-Absence) Points}
\usage{
cast_background(
  occurrences,
  study_area = NULL,
  raster_stack,
  n_bg = NULL,
  ratio = 2,
  min_bg = 500L,
  max_bg = 20000L,
  strategy = c("random", "environmental"),
  cell_thin = TRUE,
  exclude_presence = TRUE,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{occurrences}{A \code{data.frame} with at least \code{lon} and \code{lat} columns
(presence locations). May include additional columns that will be
preserved in the output.}

\item{study_area}{A \link{cast_study_area} object defining the spatial extent
for background sampling. If \code{NULL}, backgrounds are sampled from all
non-NA cells of \code{raster_stack}.}

\item{raster_stack}{A \code{terra::SpatRaster} of environmental variables.
Used both for extracting values at occurrence and background points
and for defining valid (non-NA) sampling cells.}

\item{n_bg}{Integer or \code{NULL}. Number of background points. If \code{NULL}
(default), adaptively determined as \code{clamp(ratio * n_presence, min_bg, max_bg)}.}

\item{ratio}{Numeric. Multiplier for adaptive background count.
\code{n_bg = ratio * n_presence}. Default \code{2}.}

\item{min_bg}{Integer. Minimum background points. Default \code{500L}.}

\item{max_bg}{Integer. Maximum background points. Default \code{20000L}.}

\item{strategy}{Character. Background sampling strategy:
\itemize{
\item \code{"random"} (default): uniform random sampling from valid cells.
\item \code{"environmental"}: stratified random sampling in environmental space
(partitions environmental PCA space into bins and samples uniformly
across bins).
}}

\item{cell_thin}{Logical. If \code{TRUE} (default), ensures only one
occurrence per raster cell (removes spatial duplicates at raster
resolution).}

\item{exclude_presence}{Logical. If \code{TRUE} (default), background points
cannot fall in cells occupied by occurrences.}

\item{seed}{Integer or \code{NULL}. Random seed for reproducibility.}

\item{verbose}{Logical. Print informational messages. Default \code{TRUE}.}
}
\value{
A \code{data.frame} with columns:
\describe{
\item{lon, lat}{Coordinates.}
\item{presence}{Integer (1 = presence, 0 = background).}
\item{...}{One column per layer in \code{raster_stack} with extracted values.}
}
Rows with any \code{NA} in environmental variables are removed.
}
\description{
Creates background points within a study area for species distribution
modeling, with adaptive count based on the number of presence records.
Extracts environmental variables from a raster stack and merges with
presence data to produce a ready-to-model dataset.
}
\references{
Barbet-Massin, M. et al. (2012). Selecting pseudo-absences for species
distribution models: how, where and how many?
\emph{Methods in Ecology and Evolution}, 3(2), 327-338.
}
\seealso{
\code{\link[=cast_study_area]{cast_study_area()}}, \code{\link[=cast_prepare]{cast_prepare()}}
}
