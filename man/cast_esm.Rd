% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/esm-fit.R
\name{cast_esm}
\alias{cast_esm}
\title{Ensemble of Small Models (ESM) for Rare Species}
\usage{
cast_esm(
  data,
  vars = NULL,
  top_k = 8L,
  base_algo = c("glm", "gam"),
  val_fraction = 0.25,
  response = "presence",
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{A \code{data.frame} with \code{presence} column and predictor columns.}

\item{vars}{Character vector of candidate predictor names. If \code{NULL},
uses \code{\link[=get_env_vars]{get_env_vars()}}.}

\item{top_k}{Integer. Maximum number of predictors retained before
enumerating pairs (defends against combinatorial explosion). Default
\code{8L}.}

\item{base_algo}{Character. \code{"glm"} (default) or \code{"gam"}. GAMs use
thin-plate smooths; GLMs use linear + quadratic terms.}

\item{val_fraction}{Numeric. Stratified hold-out fraction for AUC weight
estimation. Default \code{0.25}.}

\item{response}{Character. Response column. Default \code{"presence"}.}

\item{seed}{Integer. Random seed.}

\item{verbose}{Logical. Print progress.}
}
\value{
A list with class \code{"cast_esm"} containing:
\describe{
\item{models}{List of fitted bivariate sub-models.}
\item{pairs}{Matrix of column-name pairs used.}
\item{weights}{AUC-derived weights on the held-out split.}
\item{vars}{The retained predictors.}
\item{base_algo}{\code{"glm"} or \code{"gam"}.}
\item{val_auc}{Ensemble AUC on the held-out split.}
}
}
\description{
Implements the Ensemble of Small Models approach of Breiner et al. (2015)
for species with low presence counts. Fits one bivariate sub-model
(GLM or GAM) for each pair of selected predictors and combines them via
AUC-weighted averaging on a held-out validation split.
}
\details{
Designed as the recommended fallback when standard SDMs would over-fit
on rare species (typically \code{n_pres < 30}). Complementary to \code{\link[=cast_fit]{cast_fit()}}.
}
\references{
Breiner, F. T., Guisan, A., Bergamini, A., & Nobis, M. P. (2015).
Overcoming limitations of modelling rare species by using ensembles of
small models. \emph{Methods in Ecology and Evolution}, 6, 1210-1218.
}
\seealso{
\code{\link[=cast_fit]{cast_fit()}}, \code{\link[=cast_batch]{cast_batch()}}
}
